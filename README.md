# Mini-Search-Engine
A lightweight search engine implemented using the Python Programming Language which crawls the text files and searches for the presence of the keywords in accordance with  the user input in the text files present within the folder. The text files with the highest TF-IDF(Term frequency - Inverse Document Frequency) is given more importance.
